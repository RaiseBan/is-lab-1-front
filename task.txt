И так, у меня есть вот такая начальная структура проекта.
Вот index.js:
import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


И так:
задача:
это будет SPA веб сайт, который должен:
1. иметь главную страницу, на которую попадают сразу же как переходят на localhost:3000.
Главная страница доступна даже не аутентифицированным пользователям.
на главной странице должно быть:
1. header:
1.1 sign in/ sign out. Если пользователь уже аутентифицирован, то logout.
1.2 меню. Меню включает в себя следующие вкладки, по которым можно перейти:
1.2.1 таблица(По факту главная страница, которая будет содержать таблицу объектов.)
В таблице должны отображаться объекты MusicBand( все вложенные объекты должны раскрываться) и пользователь, который создал этот объект.
Данные можно получать через вебсокет, который мы делали.
1.2.2. вкладка для визуализации объектов. (Путь она будет, но пока что мы ее не делаем)
2. Body для вкладки "таблица"(главная страница) должна содержать таблицу
